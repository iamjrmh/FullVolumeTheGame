# Build cache: the artist+title -> album-art map tools/fill-covers.py sweeps
# out of Chorus Encore. Several MB, rebuildable in one command, and nothing
# the site serves - so it is not worth versioning.
tools/cover-map.json

# Same again for the Deezer lookups behind the Rock Band covers.
tools/deezer-cache.json

# OS noise
.DS_Store
Thumbs.db
desktop.ini
